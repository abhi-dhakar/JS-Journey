// Logical Operators

let isStudent = true, isAdult = false;
console.log("AND (&&):", isStudent && isAdult); // false
console.log("OR (||):", isStudent || isAdult);  // true
console.log("NOT (!):", !isStudent);            // false